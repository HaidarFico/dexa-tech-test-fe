import { MouseEvent } from "react";

function NavBar() {

    return (
        <nav className="navbar navbar-expand-lg bg-body-tertiary">
            <div className="container-fluid">
                <a className="navbar-brand" href="#">Dexa Group Roll Call Application</a>
            </div>
        </nav>
    );
}

export default NavBar